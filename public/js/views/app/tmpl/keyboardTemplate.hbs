<div class="modal-content">
  <table class="markdown-help-content">
    <tbody>
      <tr>
        <td colspan="2"><h2>Global shortcuts</h2></td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>/</code></td>
        <td>Toggle compose mode.</td>
      </tr>
      <tr>
        <td><code>q</code> or <code>r</code></td>
        <td>Quote selected text</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>f</code></td>
        <td>Focus the search input</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>g</code></td>
        <td>Focus the chat input</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>h</code></td>
        <td>Toggle help dialog</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>↓</code> or <code>↑</code></td>
        <td>Navigate up and down the left menu</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>Enter</code></td>
        <td>Go to the selected conversation</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>→</code></td>
        <td>Go to the next conversation</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>←</code></td>
        <td>Go to the previous conversation</td>
      </tr>
      <tr>
        <td><code>{{cmdKey}}</code> + <code>{{gitterKey}}</code> + <code>1</code>...<code>0</code></td>
        <td>Open conversation in numbered position</td>
      </tr>
      <tr>
        <td colspan="2">
          <h2>Input mode shortcuts</h2>
          <em>Active when focusing an input (chat or search)</em>
        </td>
      </tr>
      <tr>
        <td><code>Tab</code></td>
        <td>Focus next input</td>
      </tr>
      <tr>
        <td><code>⇧</code> + <code>Tab</code></td>
        <td>Focus previous input</td>
      </tr>
      <tr>
        <td colspan="2">
          <h2>Chat mode shortcuts</h2>
          <em>Active when focusing the chat input</em>
        </td>
      </tr>
<!--         <tr>
        <td><code>Enter</code></td>
        <td>Send message (when not in compose mode)</td>
      </tr> -->
      <tr>
        <td><code>{{cmdKey}}</code> + <code>Enter</code></td>
        <td>Send message when in compose mode</td>
      </tr>
      <tr>
        <td><code>↑</code></td>
        <td>Edit previous message (if possible)</td>
      </tr>
      <tr>
        <td colspan="2">
          <h2>Search mode shortcuts</h2>
          <em>Active when focusing the search input</em>
        </td>
      </tr>
      <tr>
        <td><code>↓</code></td>
        <td>Select next conversation in the results list</td>
      </tr>
      <tr>
        <td><code>↑</code></td>
        <td>Select previous conversation in the results list</td>
      </tr>
      <tr>
        <td><code>Enter</code> or <code>→</code></td>
        <td>Go to selected conversation in the results list</td>
      </tr>
    </tbody>
  </table>
</div>
<br>
