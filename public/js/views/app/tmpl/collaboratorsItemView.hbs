<img src="{{ avatar_url }}">
<div class="content">
  {{#if showEmailForm }}
  <form>
    <p>
      <input class="js-invite-email" type="email" placeholder="{{ text }}" required />
      <input type="submit" value="Invite" class="trpButton trpBtnGreen" />
    </p>
  </form>
  {{else}}
  <p>{{ text }}</p>
  {{/if}}
</div>

{{#if showAddButton}}
<div class="action">
  <a class="trpButton trpBtnGreen js-add">Add</a>
</div>
{{/if}}
