{{! TODO this is basically an extension of the primary-collection-view}}
<div class="room-item__container">
  {{#if roomAvatarUrl}}<img class="room-item__avatar" src="{{ roomAvatarUrl }}" />{{/if}}
  <h2 class="room-item__title">{{name}}</h2>
</div>
