/* jshint unused:true, browser:true,  strict:true */
/* global define:false */
define([], function() {

  "use strict";

  var decorator = {

    decorate: function(chatItemView) {
      // $('a[data-link-type="issue"]').addClass('issue');
      // $('a[data-link-type="mention"]').addClass('mention');
    }

  };

  return decorator;

});
