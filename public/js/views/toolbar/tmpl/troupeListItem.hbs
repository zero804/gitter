<a href="{{url}}" style="width: 100%; display: inline-block">
  <span class="github-{{githubType}}" style="margin-top: 2px">{{uri}}{{name}}</span>
  {{#if unreadItems}}
  <span class="item-unread-badge">
    <span class="trpTroupeUnreadCounter">{{unreadItems}}</span>
  </span>
  {{/if}}
</a>
