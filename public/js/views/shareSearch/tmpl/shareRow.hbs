<div class="trpInviteItem">
  <div class="cell">
    <div class="trpInviteAvatar" style="background-image: url('{{avatarUrl}}');"></div>
  </div>
    <div class="trpInviteName">
      {{displayName}}
      <span>
      {{#if username}}
      <span class="trpSmall">@{{username}}</span>
      {{/if}}
      {{#if emails}}
      <span class="trpInviteEmails">
      {{#each emails}}
      {{#unless @index}}|{{/unless}}
      {{.}}
      {{/each}}
      </span>
      {{/if}}
      </span>
    </div>
    <div class="inviteButtonContainer">
    {{#if invited}}
      <button class="trpInviteButton disabled">Invited</button>
    {{else}}
      {{#if connected}}
      <button class="trpInviteButton disabled">Connected</button>
      {{else}}
        {{#if member}}
        <button class="trpInviteButton disabled">Member</button>
        {{else}}
        <button class="trpInviteButton">Invite</button>
        {{/if}}

      {{/if}}
    {{/if}}
    </div>
</div>