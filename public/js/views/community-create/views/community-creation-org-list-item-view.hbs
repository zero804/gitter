<a href="{{ absoluteUri }}" class="community-create-list-item-link">
  <img
    class="community-create-org-list-item-avatar"
    src="{{ avatarSrcset.src }}"
    srcset="{{ avatarSrcset.srcset }}"
    height="{{ avatarSrcset.size }}"
    width="{{ avatarSrcset.size }}" />
  <span>
    {{name}}
  </span>
</a>
