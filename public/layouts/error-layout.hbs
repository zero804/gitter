<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Gitter – {{ title }}</title>
    <meta charset="utf-8">
    <meta name="google-site-verification" content="nnoWjW8hKT4umkqn1UrCdVHtLs8Q2pDi5ek0x5PUHug" />

    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
    <link rel="shortcut icon" href="{{cdn 'images/favicon-error.ico' }}">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
    <script type="text/javascript">
      window.addEventListener("message", function(e) {
        if(e.data !== "oauth_upgrade_complete") return;
        window.location.reload(true);
      }, false);

      $(document).ready(function() {
        $('#upgrade').on('click', function(e) {
          e.preventDefault();
          window.open('/login/upgrade?scopes=repo');
        });
      });
    </script>

    <style type="text/css">
      body {
        background-color: #ecf1ec;
        font-family: 'Open Sans', 'HelveticaNeue-Light', sans-serif;
      }

      .image {
         position: absolute;
         top: 40%;
         left: 30%;
         margin-left: -32px; /* Half the width */
      }

      .error {
        position: absolute;
        top: 40%;
        left: 30%;
        margin-left: 50px;
        font-family: 'Open Sans', 'Sans';
        font-size: 24px;
        color: #666;
        width: 600px;
      }

      .error-text {
        font-size: 18px;
      }

      .error-input {
        font-size: 22px;
        border: 0;
        outline: 0;
        padding: 0;
        margin: 0;
        background: transparent;
      }

      .error-input:focus {
        border: 0;
        outline: 0;

      }

      a {
        color: #2980b9;
      }
    </style>
  </head>
  <body>
    {{{ body }}}
  </body>
</html>
