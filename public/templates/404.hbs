<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Gitter - Page Not Found</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
    <meta name="google-site-verification" content="nnoWjW8hKT4umkqn1UrCdVHtLs8Q2pDi5ek0x5PUHug" />

    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
    <link rel="shortcut icon" href='{{cdn "images/gitter/favicon-error.ico" }}'>

    <script type="text/javascript">
    window.addEventListener("message", function(e) {
      if(e.data !== "oauth_upgrade_complete") return;
      window.location.reload(true);
    }, false);

    $(document).ready(function() {
      $('#upgrade').on('click', function(e) {
        e.preventDefault();
        window.open('/login/upgrade?scopes=repo');
      });
    });
    </script>


    <style type="text/css">
      body {
        background-color: #ecf1ec;
      }

      .image {
         position: absolute;
         top: 40%;
         left: 30%;
         margin-left: -32px; /* Half the width */
      }

      .error {
        position: absolute;
        top: 40%;
        left: 30%;
        margin-left: 50px;
        font-family: 'Open Sans', 'Sans';
        font-size: 24px;
        color: #666;
        width: 600px;
      }

      .error-text {
        font-size: 18px;
      }

      .error-input {
        font-size: 22px;
        border: 0;
        outline: 0;
        padding: 0;
        margin: 0;
        background: transparent;
      }

      .error-input:focus {
        border: 0;
        outline: 0;

      }

      a {
        color: #2980b9;
      }

    </style>

  </head>
  <body>
    <a href="{{homeUrl}}"><img class="image" src="{{cdn 'images/gitter/icon-logo-red-64@2x.png'}}" width="64" title="The sad face"></a>

    <div class="error">
      Gitter<br>
      <input id="error-text" class="error-input" size="55"><br><br>

      {{#if userMissingPrivateRepoScope}}
      <p>If you're looking to access a private repository, you will need to upgrade your GitHub authentication to
        allow us to access the list of your private repositories.</p>
      <p><a id="upgrade" href="/login/upgrade?scopes=repo">Click here to do this now.</a></p>
      {{/if}}

      {{#if stack}}
      <pre style="font-size:small">{{{stack}}}</pre>
      {{/if}}
    </div>


  <script type="text/javascript">
    var text = "404. this is not the chat you are looking for---------------- :(-------- sorry";
    var input = $( "#error-text" );
    input.select();
    setTimeout(startTyping, 1000, input, text);

    function startTyping(input, text) {
      for ( var i = 0; i < text.length; i++) {
        setTimeout(addText,120*i, input, text.charAt(i));
      }
    }

    function addText(i,c) {
      if (c !== '-') i.val( i.val() + c );
    }
  </script>

  </body>
</html>
