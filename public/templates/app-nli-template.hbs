<!doctype html>
<html class="no-js loading" lang="en">
<head>
  <meta http-equiv="X-UA-Compatible" content="IE=IE10" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1"/>
	<meta charset="utf-8">
	<title>{{troupeName}} - Gitter</title>
  {{#each socialMetadata}}
  <meta property="{{@key}}" content="{{this}}"/>
  {{/each}}
  <link rel="stylesheet" href='{{cdn cssFileName }}'>
  <link id="favicon" rel="shortcut icon" href='{{cdn "images/favicon5.png" }}'>
  {{#each dnsPrefetch}}
  <link rel="dns-prefetch" href="{{ this }}">
  {{/each}}
</head>
<body class="logged-out">

  {{> staging_marker}}

  {{#isMobile agent}}
    <div class="trpPortraitAlert" id='portrait-alert'>
      <div class="message">
        <img class="ipad-icon" src='{{cdn "images/icon-rotate-ipad.png"}}'>
        <p>Rotate iPad for better experience.</p>
        <img class="logo" src='{{cdn "images/menu-toggle.png" }}'>
      </div>
    </div>
  {{/isMobile}}

  <div class='wrap'>
    <nav class="nli-menu">
      <div class="nli-menu__intro">
        <div class="nli-menu__logo"></div>
        <p>Gitter provides free public chat rooms for open source repositories on GitHub.</p>
        <p>You can also use Gitter for private conversations amongst your organisation.</p>
        <a href="/login/github?action=login&source=leftmenu-1" class="trpButtonBig trpBtnGreen gtrCreateRoomButton">Login with GitHub to start talking</a>
      </div>
    </nav>
    <div class="trpFramePanel" id="iframe-panel">
      <iframe id="content-frame" src="{{chatAppLocation}}" style="display: block; width: 100%; height: 100%; border: 0;" frameBorder="0"></iframe>
    </div>
  </div>

  {{{ generateTroupeContext troupeContext }}}

  {{{ bootScript bootScriptName }}}
  {{>live_reload}}

</body>
</html>
