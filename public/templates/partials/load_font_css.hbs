<script type="text/javascript" async defer>

  function loadCss(url) {
    var link = document.createElement("link");
    link.type = "text/css";
    link.rel = "stylesheet";
    link.href = url;
    document.getElementsByTagName("head")[0].appendChild(link);
  }

  // perhaps pass the URL of that file into this template and then put it into loadCSS?
  // loadCss('//fonts.googleapis.com/css?family=Source+Sans+Pro:400,600,700,400italic,700italic');

</script>
