#!/usr/bin/env node
'use strict';

/*
INPUT:

* username
* (optional) forumUri - only required if categoryUri is not set
* (optional) categoryUri - defaults to the defaults to the default category if
                           forumUri is set
* (optional) title - defaults to fake data
* (optional) text - defaults to fake data

Adds a topic to the specified category as the specified user.
*/
