#!/usr/bin/env node
'use strict';

/*
INPUT:

* username
* groupUri
* (optional) name
* (optional) defaultCategoryName
* (optional) defaultTopicTitle

Adds a forum called "Topics" to the group as the specified user.
Adds a "General" category and sets that as the default category.
Adds a "Welcome to {group.name}" topic and sets it as the default topic.
*/
