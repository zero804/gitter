/*
import {equal} from 'assert';

import createAction from '../../../../../shared/action-creators//create-topic//submit-new-topic';
import { SUBMIT_NEW_TOPIC } from '../../../../../shared/constants/create-topic.js';

export default describe('submitNewTopic', () => {

  it('should return the right type', () => {
    equal(createAction().type, SUBMIT_NEW_TOPIC);
  });

});
*/
